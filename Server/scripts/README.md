Utility scripts for local development and maintenance.

Structure:
- `dev/` for local server helpers
- `manual/` for ad-hoc verification scripts
- root-level script files for maintenance tasks that are run directly

Common commands:
- `npm run mock`
- `npm run seed:users`
- `npm run sync:indexes`
- `npm run test:local`
